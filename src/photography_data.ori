photography
→ =Tree.map(_, (photo, fileName) => {
	...photo
	type: "photo"
	file_name: Origami.basename(fileName)
})
→ =Tree.sort(_, (photo) => photo/pub_time)
→ =Tree.map(_, {
	extension: ".md->.html"
	value: (photo) => functions/parse-markdown.js(photo)
})
→ Tree.reverse
