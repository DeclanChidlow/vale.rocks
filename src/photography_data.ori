photography
→ (_) => Tree.map(_, (photo, fileName) => {
	...photo
	type: "photo"
	file_name: Origami.basename(fileName)
})
→ (_) => Tree.sort(_, (photo) => photo/pub_time)
→ (_) => Tree.mapExtension(_, ".md->.html", {
	value: (photo) => functions/parse-markdown.js(photo)
})
→ Tree.reverse
