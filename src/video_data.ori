videos
→ =Tree.map(_, (video, fileName) => {
	...video
	type: "video"
	file_name: Origami.basename(fileName)
})
→ =Tree.sort(_, (video) => video/pub_time)
→ =Tree.map(_, {
	extension: ".md->.html"
	value: (video) => functions/parse-markdown.js(video)
})
→ Tree.reverse
