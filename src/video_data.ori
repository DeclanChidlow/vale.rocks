videos
→ (_) => Tree.map(_, (video, fileName) => {
	...video
	type: "video"
	file_name: Origami.basename(fileName)
})
→ (_) => Tree.sort(_, (video) => video/pub_time)
→ (_) => Tree.mapExtension(_, ".md->.html", {
	value: (video) => functions/parse-markdown.js(video)
})
→ Tree.reverse
